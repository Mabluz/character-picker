# Blocked Sites

Sites that return 403 errors when fetched — avoid using WebFetch on these.

| Domain | Notes |
|---|---|
| `boardgamegeek.com` | All BGG thread/file URLs blocked |
| `gamefound.com` | Campaign/product pages blocked |
